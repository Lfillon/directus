// TODO Implement rules to apply when things happen on DGE data.
//      For example when a table is define as kernel or translations, the tracking must be enable or
//      some status field should be added.

// TODO Implemente endpoints to configure the collection type inside directus system collection.

export * from "./endpoints";